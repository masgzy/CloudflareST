# TODO

> 记录暂缓实现的功能与优化，按优先级分组。做完的条目请勾选并移入 Release 说明。
> 已明确不照搬的事项（如 `-n` 加上限、CSV 去掉 BOM）不在此列。

## 高优先级

- [ ] **ICMP Ping 模式（`-ping`）**
  - 真实 ICMP RTT 测速，TCPing 被墙或不通时仍可测延迟
  - 参考：Rust 版 CloudflareST-Rust 的实现思路
  - 注意：Linux 需要 root 或 CAP_NET_RAW 权限，Windows 需要管理员（或改用系统 ICMP API）；
    涉及权限检测、降级提示与跨平台构建标签，改动面大，需单独评估
  - 2026-09 评估结论：需引入 golang.org/x/net（icmp/ipv4）新依赖 + 平台权限降级策略，
    且无 root 环境可稳定验证实测效果，暂缓；建议独立分支先行验证

- [x] **Linux IP_BIND_ADDRESS_NO_PORT（配合 `-intf`）**（v2.3.5-mod-3.3 完成）
  - 绑定源 IP 时不预先分配端口，connect 阶段才由内核分配，降低高并发下四元组碰撞概率
  - 实现于 `task/bindaddr_linux.go`（`//go:build linux`）：仅 IPv4 生效、IPv6 自动跳过、
    旧内核（<4.16）setsockopt 报错静默忽略；未绑定源 IP 的路径为 no-op

- [x] **总测速 IP 数量参数（`-qn`）+ `-getcolo` 参与 `-cfcolo` 过滤**（v2.3.5-mod-3.4 完成）
  - `-qn N`：从 IP 段按总量尽量均匀采样生成待测 IP（IPv4/IPv6 均支持，配额受段容量钳制、
    超量自动测全部），「网段=数量」条目不受影响，与 `-allip` 冲突时 `-qn` 优先；
    附带收益：IPv6 大网段终于可以用有限总量测速（原随机游走策略对大段基本不可控）
  - TCPing 下 `-getcolo` 补齐的地区码参与 `-cfcolo` 过滤（N/A 移除）；
    指定 `-cfcolo` 时自动启用 `-getcolo`，无需手动搭配
  - 与 C15 的关系：默认生成策略不变（仍为每 /24 随机一个），仅在用户显式指定 `-qn` 时采样

## 中优先级

- [x] **结果表格升级为框线样式（B8 完整版）**（v2.3.5-mod-3.3 完成）
  - 上下框线 + 表头分隔线 + 表头反色高亮，暗灰框线；中日韩列宽自适应对齐保留
  - NO_COLOR / 非 TTY 自动退化为无色框线

- [x] **彩色日志前缀（B10）**（v2.3.5-mod-3.3 完成）
  - `[信息]` 青、`[错误]` 红、`[警告]` 黄、`[提示]` 黄、`[调试]` 紫红（原红/黄混用，统一区分）
  - 仅前缀着色、正文默认色；`utils.Info/Warn/Error/Tip/Debugf` 统一 API
  - NO_COLOR / 非 TTY 自动退化为纯文本前缀

- [x] **进度条渲染优化（B11 残余项）**（v2.3.5-mod-3.3 完成）
  - TTY 刷新间隔 40ms → 16ms（约 60fps）
  - 渲染写入失败检测 EPIPE：停止渲染并安静退出（fd1 的 EPIPE 由 Go 运行时按
    SIGPIPE 直接终止，属预期 Unix 行为；分支覆盖其余场景）

- [ ] **CIDR 均匀分区间采样作为默认策略（C15）**
  - 将「均匀分区间采样 + 流式生成」推广为默认 IP 生成方式（当前仅 `网段=数量` 语法使用）
  - 注意：偏测速逻辑，需先与现有「每个 /24 随机取一个」策略做覆盖度/速度对比基准再决定
  - 2026-09 评估结论：改默认策略影响所有用户的 IP 覆盖面，本环境无法提供代表性基准
    （需要多地区、多次大规模实测），暂缓，待收集到可信基准数据后再评估

## 低优先级

- [ ] **下载速度中位数-MAD 加权（C16）**
  - 500ms 窗口采样、中位数抗突发；偏测速统计口径，需实测评估收益
  - 2026-09 评估结论：需在真实异构带宽（上下行抖动、限速出口）环境多轮对比才能验证收益，
    当前测试环境单一，暂缓

- [x] **文档：Mermaid 时序图 / 流程图**（v2.3.5-mod-3.3 完成）
  - README 新增「测速流程图」章节：整体流程 flowchart + 单 IP 测速 sequenceDiagram

- [ ] **一键安装脚本（安卓 Termux / OpenWrt）**
  - 参考带 setup.sh 包装的仓库做法，为本仓库维护一份按架构自动下载的安装脚本
  - 2026-09 评估结论：维护者明确暂不做一键脚本，仅保留条目备忘

- [x] **README 增加测速结果示例截图**（v2.3.5-mod-3.3 完成）
  - `docs/result_example.png`：真实测速数据渲染（HSV 进度条 + 框线表格 + 实时速度），
    由 pty 捕获 ANSI 流后逐帧回放渲染，随终端样式更新需重新生成

- [ ] **评估 `-f` 与 `-ip` 同时指定时合并来源**
  - 当前行为：`-ip` 优先、`-f` 被忽略（与上游一致）；
    合并后更灵活但属于行为变更，需先收集用户反馈
